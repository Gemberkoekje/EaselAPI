# Pass 8 - the trees in the water, weighted to the right so the bird keeps a
# light field behind it; and incident on the open water.
m_tree_refl()
m_surface()
print(s.look())
print(s.look(values=True))
