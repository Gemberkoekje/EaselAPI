# the deck was the second-brightest thing in a night picture, which left the water
# no range to be dark in. Re-run the stack in depth order: floor, then water on it.
floor()
water()
print(s.look(values=True))
print(s.look())
