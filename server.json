{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.Gemberkoekje/easel",
  "title": "Easel",
  "description": "A headless painting engine for AI agents: brushes, paint load, wet blending, canvas texture.",
  "version": "0.1.0",
  "websiteUrl": "https://github.com/Gemberkoekje/EaselAPI#mcp-server",
  "repository": {
    "url": "https://github.com/Gemberkoekje/EaselAPI",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "pypi",
      "registryBaseUrl": "https://pypi.org",
      "identifier": "easel-paint",
      "version": "0.1.0",
      "runtimeHint": "uvx",
      "transport": {
        "type": "stdio"
      },
      "runtimeArguments": [
        {
          "type": "named",
          "name": "--from",
          "value": "easel-paint[mcp]",
          "description": "The whole launch command is: uvx --from \"easel-paint[mcp]\" easel-mcp. Two things make it longer than usual. The MCP server is an optional extra, because nothing else in the engine imports mcp and a painter working from Python or the shell never needs its 28 dependencies; and the console script is easel-mcp, not easel-paint, because the package is a painting engine of which the server is one face. A client that cannot assemble this should run that command verbatim.",
          "isRequired": true
        }
      ],
      "packageArguments": [
        {
          "type": "named",
          "name": "--dir",
          "description": "Directory the server reads and writes .easel session files, looks and exported PNGs in. Defaults to the working directory.",
          "format": "filepath",
          "isRequired": false
        }
      ]
    }
  ]
}
